Copy seurat_clustered.rds from Step 5 Output into this Input folder.

For manual annotation, also place one CSV whose name contains
"cluster" and "annotation". Required columns:
    cluster,cell_type

A template is included. If the template is not completed, the script preserves
labels such as Cluster_0, Cluster_1, etc.
